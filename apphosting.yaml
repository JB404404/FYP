# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 3
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
env:
#   Configure environment variables.
#   See https://firebase.google.com/docs/app-hosting/configure#user-defined-environment
#   - variable: MESSAGE
#     value: Hello world!
#     availability:
#       - BUILD
#       - RUNTIME

#   Grant access to secrets in Cloud Secret Manager.
#   You can create secrets with the CLI command `firebase apphosting:secrets:set <secretName>`.
#   See https://firebase.google.com/docs/app-hosting/configure#secret-parameters
  - variable: NEXT_PUBLIC_OAUTH_CLIENT_KEY
    secret: NEXT_PUBLIC_OAUTH_CLIENT_KEY
  - variable: NEXT_PUBLIC_OAUTH_CLIENT_ID
    secret: NEXT_PUBLIC_OAUTH_CLIENT_ID
  - variable: NEXT_PUBLIC_GOOGLE_API_KEY
    secret: NEXT_PUBLIC_GOOGLE_API_KEY
  - variable: NEXT_PUBLIC_EMBEDDED_MAP_API_KEY
    secret: NEXT_PUBLIC_EMBEDDED_MAP_API_KEY
